import {} from './actions';

const inicialState = {
    
}; 
const reducer = (state = inicialState, action) => {
    switch (action.type) {

    
    default: return state 
}
}
export default reducer; 
